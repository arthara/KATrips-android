<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.katripstask.katrips">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Starter"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".about_us"></activity>
        <activity android:name=".splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".modul.login.LoginActivity" />
        <activity android:name=".modul.caritiket.CariTiketActivity" />
        <activity android:name=".modul.pilihtiket.PilihTiketActivity" />
        <activity android:name=".modul.inputpesanan.InputPesananActivity" />
        <activity android:name=".modul.konfirmasitiket.KonfirmasiTiketActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>